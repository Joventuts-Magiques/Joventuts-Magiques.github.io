services:
  jekyll:
    image: jekyll/jekyll:4.2.2
    command: jekyll serve --watch --host 0.0.0.0 --incremental
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
